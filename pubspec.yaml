name: project_mobile
description: recipe reccomendation

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  shared_preferences: ^2.2.2
  url_launcher: ^6.2.1
  cached_network_image: ^3.3.1
  sqflite: ^2.3.0
  path: ^1.8.3
  crypto: ^3.0.3
  geolocator: ^9.0.2
  geocoding: ^2.1.1
  google_maps_flutter: ^2.5.0
  permission_handler: ^11.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/